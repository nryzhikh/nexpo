# ui-primitives

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build ui-primitives` to build the library.

## Running unit tests

Run `nx test ui-primitives` to execute the unit tests via [Jest](https://jestjs.io).
